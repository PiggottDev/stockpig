plugins {
    id 'java'
    id 'application'
}

group 'piggott'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.6.0'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine'
}

run {
    mainClass = 'piggott.chess.gui.StockpigGUI'
    standardInput = System.in
}

test {
    useJUnitPlatform()
}